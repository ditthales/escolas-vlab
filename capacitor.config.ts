import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.thalesfraga.escolasvlab',
  appName: 'escolas-vlab',
  webDir: 'www',
  server: {
    androidScheme: 'http'
  }
};

export default config;
